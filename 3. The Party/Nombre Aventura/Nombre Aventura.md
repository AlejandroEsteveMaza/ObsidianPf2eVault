
```dataview
table Player, Class, Race, level, Role
from "3. The Party/Nombre Aventura"
where (Role = "Player") 
where (Status = "Active") 
```

```dataview
table Player, hp, ac, modifier, pasperc As "Passive Perception (WIS)"
where (Role = "Player") 
where (Status = "Active") 
```
